{
  "name": "mustsolve-backend",
  "version": "1.0.0",
  "description": "Backend API for MustSolve coding platform with PostgreSQL database integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:test": "node -e \"require('dotenv').config(); require('./src/services/database').testConnection().then(() => process.exit(0)).catch(() => process.exit(1));\""
  },
  "keywords": [
    "coding",
    "algorithms",
    "interview",
    "practice",
    "postgresql",
    "api"
  ],
  "author": "MustSolve Team",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
